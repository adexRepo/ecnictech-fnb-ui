import 'package:flutter/material.dart';

class DialogAddHome extends StatelessWidget {
  const DialogAddHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
